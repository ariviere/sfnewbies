<%= form_for([place, @comment]) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	
	<h3>Commenter</h3>
  <div class="field">
    <%= f.text_area :content, cols: 80, rows: 10 %>
  </div>
  <div class="actions">
    <%= f.submit "Envoyer", class: "btn btn-primary" %>
  </div>
<% end %>
