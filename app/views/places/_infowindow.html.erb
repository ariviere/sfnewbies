<%= image_tag(place.image.thumb, :alt => place.name, :class => "pull-left cadre-image") %>
<p><b><%= link_to place.name, place_path(place) %></b><br /> 
<%= link_to "#{pluralize(place.comments.count, 'commentaire', 'commentaires')}", place_path(place, :anchor => "comments") %>
<%= place.description %></p>
